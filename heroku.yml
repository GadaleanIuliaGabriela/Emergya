setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: prod/Dockerfile
  config:
    CMS_ENVIRONMENT: prod
    SITE_INSTALL: 1
run:
  config:
    CMS_ENVIRONMENT: prod
    SITE_INSTALL: 1
